export function PageStub({ title }) {
  return (
    <div className="mx-auto max-w-3xl px-4 py-16 text-center">
      <h1 className="text-3xl">{title}</h1>
      <p className="mt-4 text-text-muted">
        This part of PlayLearn is being built next.
      </p>
    </div>
  );
}
